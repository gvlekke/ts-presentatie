const typedVar: string = "this is a typed variable";

interface SomeTypeOfAnObject {
  var1: string;
  var2: number;
  var3: boolean;
}

const typedObj: SomeTypeOfAnObject = {
  var1: "this is a string",
  var2: 2,
  var3: true,
};
